# 笔锋核心功能设计文档

> 定位：基于 AI 的高中写作能力渐进训练系统。
> 原则：训练导向，不是作文导向；AI 只做教练，不做枪手。

---

## 一、核心理念

### 1.1 产品定位

笔锋不是“作文批改工具”，而是一个**将写作能力拆分为最小训练单元、逐层递进的训练系统**。学生每次只聚焦一个能力点，通过高频、短耗时的微训练，逐步建立完整的写作能力。

### 1.2 两条铁律

1. **不代写**：AI 在任何环节都不直接给出可照抄的答案。只提供引导、评价和对比。
2. **不设锁**：训练层级没有”必须通关才能进入下一关”的硬门槛。学生自主流动，系统只做推荐。

### 1.3 十二大模块

写作能力提升是一个完整的系统工程，由十二大模块协同支撑：

| 类型 | 模块 | 作用 | 在系统中的位置 |
|------|------|------|---------------|
| **核心四模块** | 阅读输入 | 积累素材、语感、语言模型 | 训练前的”范文赏析”环节 |
| | 思维训练 | 审题、提纲、逻辑链条 | 独立模块 + 融入训练链 |
| | 刻意练习 | 逐层递进的能力训练 | 核心训练链（L1-L7） |
| | 复盘反思 | 巩固提升、追踪进步 | 每次训练后的训练报告 |
| **支撑四模块** | 示范模仿 | 看到差距、理解方法 | 训练后的对比学习环节 |
| | 间隔重复 | 对抗遗忘、巩固能力 | 自动调度复习任务 |
| | 自我评估 | 从依赖 AI 到独立写作 | 融入训练流程 |
| | 动机维持 | 保持学习动力和习惯 | 首页 + 全局机制 |
| **教学支撑模块** | 评分标准显式化 | 学生知道目标，有的放矢 | 每次训练前展示 |
| | 错误类型学 | 精准诊断，针对性训练 | AI 评审 + 错误档案 |
| | 写作过程教学 | 教方法，不只是给反馈 | 融入训练流程 |
| | 文体意识训练 | 不同文体不同规则 | 按文体分类训练 |

每个训练单元的标准流程围绕这十二大模块设计（详见第三节）。

### 1.4 能力阶段标签（系统内部使用，不对学生展示）

| 阶段 | 标签 | 语文起始层级 | 英语起始层级 | 特征 |
|------|------|-------------|-------------|------|
| 萌芽期 | 🌱 | L1-L3 | L1-L2 | 跑题频繁，结构混乱，语言口语化 |
| 生长期 | 🌿 | L2-L5 | L2-L4 | 基本不跑题，结构完整但平庸 |
| 茁壮期 | 🌳 | L4-L7 | L4-L6 | 结构清晰，有亮点但不稳定 |

### 1.5 年级维度（训练策略的核心变量）

学生不只是能力阶段不同，**年级**决定了完全不同的训练策略：

| 维度 | 高一 | 高二 | 高三 |
|------|------|------|------|
| 时间充裕度 | 充裕 | 中等 | 紧迫 |
| 核心目标 | 打基础、养习惯、建素材库 | 针对性提升、查漏补缺 | 冲刺提分、考场实战 |
| 训练重心 | 八大模块均衡发展 | 强化薄弱点、增加全文训练 | 限时模拟、考试策略、心理训练 |
| 推荐训练节奏 | 每天 1-2 个微训练（10-15 分钟） | 每天 2-3 个训练（20-30 分钟） | 每天 3-4 个训练 + 周末模拟（40-60 分钟） |
| 间隔重复间隔 | 正常节奏（1/3/7/14/30 天） | 稍紧凑（1/2/5/10/20 天） | 紧凑（1/2/3/7/14 天） |
| 限时策略 | 萌芽期不限时 → 逐渐加压 | 直接按阶段时间递进 | 全程限时，模拟考场 |

**年级 × 能力阶段矩阵**：

```
           萌芽期          生长期          茁壮期
高一     基础夯实         稳步提升         提前冲刺
         从L1开始，        均衡发展，        可挑战高难度
         不急不躁         建立素材库        训练

高二     快速补课         针对性突破        全面提升
         跳过已掌握的     集中攻克薄弱     全文训练为主
         直接从薄弱点     点               争取稳定发挥

高三     紧急救火         强化训练          冲刺高分
         只练最能提分     全面强化，        限时模拟，考试
         的能力点         消灭短板          策略，心理训练
```

**系统如何使用年级信息**：

1. **新用户注册时**：收集年级信息（高一/高二/高三）
2. **训练推荐**：根据年级 + 能力阶段，推荐最适合的训练组合
3. **时间设定**：根据年级自动调整训练时间压力
4. **复习调度**：根据年级调整间隔重复间隔
5. **首页展示**：高一侧重"今日学习"，高三侧重"今日冲刺+复习提醒"
6. **AI 反馈风格**：高一鼓励为主，高三直接指出问题+给解决方案

---

## 二、预评分系统（能力诊断）

### 2.1 强制规则

- **新用户首次使用**：必须完成预评分（约 10-15 分钟），获得初始能力画像后才能进入训练。
- **复评触发**：系统检测到学生连续 5 次训练得分均高于当前阶段阈值，自动提示“你的能力可能已提升，建议重新诊断”。

### 2.2 诊断内容（5 个微任务）

| 任务 | 题量 | 时长 | 测量能力 |
|------|------|------|---------|
| 审题立意 | 3 题 | 3 分钟 | 方向感：能否抓住题目核心，提出不跑题的立意 |
| 段落排序 | 1 题 | 2 分钟 | 结构感：能否识别段落间的逻辑关系 |
| 词汇选择 | 5 题 | 3 分钟 | 语言精度：能否在语境中选最准确/高级的词 |
| 句式改写 | 3 题 | 4 分钟 | 句式能力：能否将简单句升级为复合句/倒装句等 |
| 找错误 | 3 题 | 3 分钟 | 规范意识：能否识别逻辑/语法/表达问题 |

### 2.3 输出

- **能力雷达图**：5 个维度得分
- **阶段标签**：萌芽/生长/茁壮
- **推荐起始层级**：语文 Lx + 英语 Lx

---

## 三、训练体系

### 3.1 语文训练链（7 层）

| 层级 | 名称 | 时长 | 学生输出 | 训练目标 |
|------|------|------|---------|---------|
| L1 | 审题立意 | 3-5 分钟 | 关键词提取 + 一句话中心论点 | 确保不跑题，建立“先想后写”的习惯 |
| L2 | 段落功能卡 | 5 分钟 | 5 段式骨架（每段一句话功能说明） | 建立议论文结构直觉 |
| L3 | 开头段专项 | 10 分钟 | 150 字开头段 | 引人入胜 + 点明论点 |
| L4 | 论证段专项 | 15 分钟 | 200 字完整论证段 | 论点 → 论据 → 分析，逻辑闭环 |
| L5 | 过渡段专项 | 10 分钟 | 一段过渡文字 | 段间衔接自然，逻辑推进 |
| L6 | 结尾段专项 | 10 分钟 | 升华结尾 | 跳出个人，上升到群体/时代 |
| L7 | 全文整合 | 45-60 分钟 | 800 字完整作文 | 综合运用，限时成文 |

### 3.2 英语训练链（6 层）

| 层级 | 名称 | 时长 | 学生输出 | 训练目标 |
|------|------|------|---------|---------|
| L1 | 句式仿写 | 5 分钟 | 3 个改写句子 | 掌握高级句式（倒装/强调/分词/虚拟） |
| L2 | 段落骨架 | 10 分钟 | 一段 PEEL 结构段落 | 主题句 + 论据 + 解释 + 过渡 |
| L3 | 应用文格式 | 10 分钟 | 一封完整应用文 | 掌握书信/演讲/通知格式 |
| L4 | 读后续写开头 | 10 分钟 | 续写首段 | 衔接前文，保持风格一致 |
| L5 | 语法纠错 | 5 分钟 | 改正 3-5 处错误 | 识别常见语法陷阱 |
| L6 | 全文写作 | 30-40 分钟 | 120-150 词完整作文 | 综合运用，限时成文 |

### 3.3 每个训练单元的标准结构（十二大模块闭环）

```
【评分标准显式化】展示本次训练的评分标准
  → 【阅读输入】题目/任务描述
    → [可选] 查看范文赏析（AI 推荐 2-3 段精选片段 + 赏析引导）
      → 【自我评估】学生先快速标注自己认为的问题（中高层级可选）
        → 【思维训练】审题立意 / 提纲编写（限时或不限时，依阶段而定）
          → 【刻意练习】学生写作/作答
            → 【复盘反思】AI 评审 + 训练报告（含错误类型分析）
              → [可选] 示范模仿（查看修改建议 / 对比范文）
                → 学生选择：再练一次 / 进入下一层 / 查看详细解析
```

**全局机制（贯穿所有训练单元）**：

- **间隔重复**：系统自动调度复习任务，优先推荐到期的复习
- **动机维持**：每日微目标、连续训练、成就解锁
- **写作过程教学**：融入训练流程，教方法不只是给反馈
- **文体意识训练**：按文体分类训练，建立文体意识
- **错误类型学**：AI 评审时自动分类错误，形成错误档案

**说明**：
- 每个训练单元都包含十二大模块的完整闭环
- 阅读输入、自我评估（中高层级）、示范模仿是"可选但推荐"的环节
- 思维训练和刻意练习是必做环节
- 复盘反思在每次训练后自动生成
- 间隔重复和动机维持是全局机制，不绑定单个训练单元

### 3.4 阅读输入模块（范文赏析）

**核心理念**：写作的底层是阅读量。学生写不好往往不是不会写，而是脑子里没有足够的语言素材和思维模型。

**应用场景**：

1. **训练前导入**：练 L3 开头段时，先给学生看 2-3 个优秀开头段范例，再让他练
2. **主题阅读包**：围绕训练主题（如"勇气"）推送相关范文片段
3. **赏析训练**：不只是读，还要分析"为什么写得好"——AI 引导学生拆解范文的结构、修辞、论证手法

**功能设计**：

| 功能 | 说明 |
|------|------|
| 范文片段推送 | 每次训练前可选"先看范文"，AI 根据当前训练层级推送 2-3 段精选片段 |
| 赏析引导 | AI 提问："这段开头用了什么手法？效果如何？"引导学生主动分析 |
| 素材收藏 | 学生可收藏好词好句、典型事例，形成个人素材库 |
| 素材应用反馈 | 学生在写作中使用收藏的素材时，AI 会提示"你用的这个素材和范文中的用法对比..." |

**原则**：
- 范文必须与当前训练层级匹配（练开头段就推开头段范文）
- AI 引导分析，不直接告诉答案
- 素材库按主题/文体/能力点分类，方便检索

### 3.5 思维训练模块（审题提纲专项）

**核心理念**：写作本质是思维的外化。很多学生的问题不是语言能力，而是审题抓不住核心、论证逻辑断裂、观点单薄缺乏层次。

**训练内容**：

| 训练类型 | 时长 | 学生输出 | 训练目标 |
|---------|------|---------|---------|
| 审题立意 | 3-5 分钟 | 一句话核心论点 + 2-3 个分论点方向 | 抓住题目核心矛盾，避免跑题 |
| 段落排序 | 2-3 分钟 | 给出正确排序并说明理由 | 理解段落间的逻辑关系 |
| 论证链条 | 5-8 分钟 | 画出"论点→论据→结论"的逻辑链 | 逻辑闭环，避免跳跃 |
| 提纲编写 | 10-15 分钟 | 完整的段落提纲（每段一句话功能说明） | 结构化思考，先想清再写好 |
| 多角度分析 | 5-8 分钟 | 从 2-3 个不同角度分析同一话题 | 思维深度，避免单一视角 |

**与训练链的关系**：
- 思维训练是训练链的"预热"和"补充"
- 学生可以在进入训练链前先做思维训练
- 也可以在训练链遇到困难时回到思维训练巩固
- 思维训练的成绩独立记录，形成"思维能力"维度

**AI 介入方式**：
- 审题训练：AI 给出题目，学生给出立意，AI 分析"这个立意的优劣是什么？有没有更好的角度？"
- 提纲训练：AI 给出学生提纲，指出"第三段和第四段之间逻辑跳跃了，读者会困惑"
- 多角度分析：AI 追问"如果从反面看呢？如果从群体角度看呢？"

### 3.6 复盘反思模块（训练报告）

**核心理念**：练完不复盘等于白练。系统需要帮学生把零散的训练经历整合成清晰的成长轨迹。

**训练报告内容**：

每次训练结束后生成，包含：

| 内容 | 说明 |
|------|------|
| 本次训练总结 | 练了什么能力点、得分如何、与上次对比 |
| AI 核心反馈 | 最关键的 1-2 条改进建议（不是泛泛建议） |
| 错题/薄弱点追踪 | 识别出的薄弱环节，自动加入"待巩固"列表 |
| 下次推荐 | 基于当前水平推荐下一步训练 |

**阶段性回顾**（每周/每阶段）：

| 内容 | 说明 |
|------|------|
| 能力曲线变化 | 4 维度历史趋势对比（本周 vs 上周） |
| 训练量统计 | 本周练了多少次、覆盖了哪些能力点 |
| 进步亮点 | AI 指出"你在 X 方面进步明显" |
| 待攻克难点 | "Y 方面还需要加强，建议..." |
| 复习建议 | 基于薄弱点推荐复习哪些已学层级 |

**数据支撑**：
- 每次训练的评分（4 维度）存入数据库，形成时间序列
- 薄弱点自动识别：连续 3 次某维度得分低于阈值，标记为"薄弱"
- 进步识别：连续 2 次得分提升超过 5 分，标记为"进步中"

**原则**：
- 报告语言具体、 actionable，不空泛
- 强调进步而非不足，维持学习动力
- 报告可随时查看，支持历史回溯

### 3.7 示范模仿模块（对比学习）

**核心理念**：光知道"写得不好"不够，还要知道"好的长什么样"。通过对比学习，让学生看到差距、理解方法。

**功能设计**：

| 功能 | 说明 |
|------|------|
| 修改前后对比 | AI 根据学生作文生成"修改建议版"，展示修改前后的 diff |
| 范文对比 | 学生作文 vs 同题高分范文，并排展示，AI 指出关键差异 |
| 典型错误案例 | 系统维护"常见错误库"，学生犯错时推送相关案例及修改方法 |
| 句式升级对比 | 针对英语：学生的简单句 vs 升级后的复杂句，展示句式变化 |

**AI 介入原则**：
- 修改建议版：AI 只修改最需要改进的 1-2 处，不全面重写
- 对比分析：AI 引导学生自己发现差异，而非直接告诉答案
- 错误案例：展示"错误→正确"的对比，附带规则说明

**与训练链的结合**：
- 每个训练层级完成后，可选"查看修改建议"或"对比范文"
- 修改建议版可作为"再练一次"的参考（但不能直接抄）
- 范文对比在阅读输入模块中也有涉及，这里是更聚焦的对比分析

### 3.8 间隔重复模块（对抗遗忘）

**核心理念**：练完就忘是常态。间隔重复通过在学生即将遗忘时主动推送复习，将短期训练成果转化为长期能力。

**调度策略**：

基于艾宾浩斯遗忘曲线，每个能力点的复习间隔：

| 复习轮次 | 间隔 | 触发条件 |
|---------|------|---------|
| 第 1 次 | 训练后 1 天 | 自动推送 |
| 第 2 次 | 训练后 3 天 | 自动推送 |
| 第 3 次 | 训练后 7 天 | 自动推送 |
| 第 4 次 | 训练后 14 天 | 自动推送 |
| 巩固期 | 间隔拉长至 30 天 | 能力稳定后 |

**实现方式**：

- 每次训练后，系统记录"能力点 + 训练时间 + 得分"
- 每日打开系统时，检查是否有到期的复习任务
- 推荐首页显示"今日复习"入口，优先级高于新训练
- 复习任务与新训练混合推荐，避免枯燥

**复习形式**：

- 不是简单重复同一道题，而是**同能力点的变体题**
- 例如：上次练了"勇气"主题的论证段，复习时推"坚持"主题的论证段
- AI 评审时重点关注"上次薄弱点是否改善"

**数据追踪**：

```
{
  "abilityPoint": "L4-论证段",
  "lastPracticed": "2026-05-20",
  "score": 72,
  "reviewCount": 2,
  "nextReview": "2026-05-27",
  "status": "reviewing"  // new / learning / reviewing / mastered
}
```

**掌握判定**：

| 状态 | 条件 |
|------|------|
| new | 未训练过 |
| learning | 已训练但间隔复习未完成 |
| reviewing | 正在间隔复习中 |
| mastered | 连续 3 次复习得分 ≥ 80，且间隔 ≥ 14 天 |

### 3.9 自我评估模块（从依赖到独立）

**核心理念**：学生最大的问题不是"写得不好"，而是"不知道自己写得不好"。系统需要训练学生的自我诊断能力，从依赖 AI 反馈过渡到独立写作。

**训练方式**：

| 训练类型 | 说明 |
|---------|------|
| 自评 + AI 评 | 学生先自己标注文章问题，再看 AI 反馈，对比差异 |
| 盲评训练 | 看一篇匿名作文，判断问题在哪里，然后看标准答案 |
| 修改预判 | AI 指出问题后，学生先预测修改方向，再看 AI 建议 |

**自评流程**：

```
学生完成写作
  → 系统提示："请先用 1 分钟快速标注你认为最需要改进的 1-2 处"
    → 学生标注（高亮原文 + 简短说明）
      → AI 评审
        → 对比展示：学生的自评 vs AI 的反馈
          → AI 分析："你标注的 X 确实是问题，但漏掉了更重要的 Y"
```

**评分维度**：

- **自评准确率**：学生标注的问题与 AI 反馈的重合度
- **自评覆盖度**：学生标注了几个问题 vs AI 识别了几个问题
- **自评优先级**：学生认为最严重的问题是否与 AI 判断一致

**阶段性目标**：

| 阶段 | 自评准确率目标 |
|------|--------------|
| 萌芽期 | ≥ 30%（能识别最基本的问题） |
| 生长期 | ≥ 50%（能识别大部分明显问题） |
| 茁壮期 | ≥ 70%（能准确诊断多数问题） |

**与训练链的结合**：

- L1-L3（低层级）：强制自评，培养习惯
- L4-L6（中高层级）：可选自评，鼓励但不强制
- L7（全文）：强制自评，作为独立写作能力的验证

### 3.10 动机维持模块（保持学习动力）

**核心理念**：高中生容易半途而废。系统需要通过降低启动成本、可视化进步、适度激励来维持学习动力。

**机制设计**：

| 机制 | 说明 | 实现方式 |
|------|------|---------|
| 每日微目标 | "今天只练 5 分钟"降低启动成本 | 首页显示今日推荐，标注预计时长 |
| 进步可视化 | 看到自己的成长轨迹 | 月度对比（上月 vs 本月的能力曲线） |
| 连续训练 | 鼓励每日打卡 | 记录连续训练天数，显示在首页 |
| 成就解锁 | 阶段性里程碑 | "首次完成全文""连续训练 7 天""能力点首次达标"等 |
| 错题本 | 不回避薄弱点 | 薄弱点集中展示，化被动为主动 |

**每日微目标设计**：

```
首页"今日推荐"卡片：
┌─────────────────────────────────┐
│ 📝 今日训练（预计 5 分钟）        │
│                                  │
│ 语文 L4 论证段专项               │
│ 上次得分 72，再练一次可能更好     │
│                                  │
│ [开始训练]  [查看其他训练]        │
└─────────────────────────────────┘
```

**进度可视化**：

```
我的成长轨迹
┌─────────────────────────────────────────┐
│ 本月 vs 上月                             │
│                                          │
│ 语文  内容 ████████░░ → █████████░  +12  │
│       结构 ██████░░░░ → ████████░░  +18  │
│       语言 █████░░░░░ → ██████░░░░  +10  │
│       规范 ███████░░░ → ████████░░  +11  │
│                                          │
│ 英语  ...                                │
└─────────────────────────────────────────┘
```

**成就系统**：

| 成就 | 条件 | 类型 |
|------|------|------|
| 初试锋芒 | 完成首次训练 | 一次性 |
| 坚持不懈 | 连续训练 7 天 | 连续 |
| 月度之星 | 单月训练 ≥ 20 次 | 月度 |
| 全面发展 | 语文和英语各完成 ≥ 3 个层级 | 一次性 |
| 首次达标 | 某能力点首次 ≥ 80 分 | 一次性 |
| 攻克难关 | 薄弱点从 <60 提升到 ≥75 | 一次性 |

**原则**：

- 激励以"进步"为核心，而非"分数"
- 不搞排名，避免学生间比较带来的焦虑
- 成就解锁时给予 AI 的个性化鼓励（"你在论证方面进步很大，继续保持！"）

---

### 3.11 评分标准显式化模块

**核心理念**：学生需要明确知道"好作文的标准是什么"，才能有针对性地努力。评分标准不是神秘的黑箱，而是可以学习和掌握的规则。

**语文议论文评分标准**：

| 等级 | 分数段 | 立意 | 结构 | 论证 | 语言 |
|------|--------|------|------|------|------|
| 一类卷 | 54-60 | 深刻独到，有思辨 | 精巧严谨，详略得当 | 论据典型，分析透彻 | 有文采，有个性 |
| 二类卷 | 42-53 | 正确明确 | 完整清晰 | 论据恰当，分析合理 | 通顺得体 |
| 三类卷 | 30-41 | 基本正确，略有偏颇 | 基本完整 | 论据一般，分析浅显 | 基本通顺 |
| 四类卷 | 30以下 | 偏题或离题 | 结构混乱 | 论据不当，缺乏分析 | 不通顺 |

**英语作文评分标准**：

| 等级 | 分数段 | 内容 | 结构 | 语言 | 规范 |
|------|--------|------|------|------|------|
| 优秀 | 22-25 | 内容充实，观点明确 | 结构清晰，衔接自然 | 句式多样，用词准确 | 格式正确，无语法错误 |
| 良好 | 18-21 | 内容较充实 | 结构较清晰 | 句式较多样 | 基本正确 |
| 一般 | 13-17 | 内容基本完整 | 结构基本完整 | 句式单一，有少量错误 | 有错误 |
| 较差 | 12以下 | 内容不完整 | 结构混乱 | 错误较多 | 格式错误 |

**功能设计**：

| 功能 | 说明 |
|------|------|
| 标准展示 | 每次训练前展示本次训练的评分标准，让学生知道目标 |
| 标准解读 | AI 用通俗语言解释每个维度的要求 |
| 标准对照 | 评审时明确指出"你在哪个维度达到了什么等级" |
| 标准进步 | 展示"你从三类卷提升到了二类卷"的进步轨迹 |

**AI 解读 Prompt**：
```
请用通俗易懂的语言向高中生解释以下评分标准：

{评分标准}

要求：
1. 每个维度用一句话说清楚核心要求
2. 举一个正面例子和一个反面例子
3. 不要用术语，用学生能理解的语言
```

### 3.12 错误类型学模块

**核心理念**：老师能立刻识别错误模式，学生需要学会"我的问题属于哪一类"，才能针对性改进。

**语文常见错误分类**：

| 类型 | 子类 | 典型表现 | 对应训练 |
|------|------|---------|---------|
| **逻辑类** | 偷换概念 | "勇气就是勇敢" | L4 论证段 |
| | 以偏概全 | "我身边的人都这样" | L4 论证段 |
| | 因果倒置 | "成功导致努力" | L4 论证段 |
| | 循环论证 | "A是因为B，B是因为A" | L4 论证段 |
| **结构类** | 详略不当 | 该详写的略写了 | L2 段落功能卡 |
| | 虎头蛇尾 | 开头精彩结尾草率 | L6 结尾段 |
| | 段落失衡 | 某段过长或过短 | L2 段落功能卡 |
| | 缺乏过渡 | 段间跳跃 | L5 过渡段 |
| **语言类** | 口语化 | "我觉得这个事挺好的" | L3-L6 片段写作 |
| | 用词不当 | "给予"写成"给与" | L3-L6 片段写作 |
| | 句式单调 | 全是简单句 | L3-L6 片段写作 |
| **规范类** | 错别字 | 常见易错字 | L7 全文 |
| | 标点错误 | 逗号句号混用 | L7 全文 |
| | 格式问题 | 议论文写成记叙文 | L2 段落功能卡 |

**英语常见错误分类**：

| 类型 | 子类 | 典型表现 | 对应训练 |
|------|------|---------|---------|
| **语法类** | 主谓不一致 | "He go to school" | L5 语法纠错 |
| | 时态错误 | "Yesterday I go" | L5 语法纠错 |
| | 冠词缺失 | "I like apple" | L5 语法纠错 |
| | 介词错误 | "depend of" | L5 语法纠错 |
| **句式类** | 句式单一 | 全是简单句 | L1 句式仿写 |
| | 中式英语 | "I very like it" | L1 句式仿写 |
| | 长句断裂 | 一个句子拆成两个 | L1 句式仿写 |
| **内容类** | 内容空洞 | 缺乏具体论据 | L2 段落骨架 |
| | 逻辑跳跃 | 论点和论据不匹配 | L2 段落骨架 |
| **格式类** | 书信格式错误 | 称呼和落款不对 | L3 应用文 |

**错误追踪数据模型**：
```typescript
interface ErrorRecord {
  id: string
  userId: string
  trainingId: string
  errorType: string      // '逻辑类-偷换概念'
  location: string       // '第三段第二句'
  originalText: string
  explanation: string
  severity: 'high' | 'medium' | 'low'
  isRecurring: boolean   // 是否反复出现
  firstOccurrence: Date
  lastOccurrence: Date
  count: number
}
```

**错误模式分析**：
```typescript
function analyzeErrorPatterns(errors: ErrorRecord[]): ErrorPattern[] {
  // 按类型分组
  const grouped = groupBy(errors, 'errorType')

  // 识别反复出现的错误
  const recurring = Object.entries(grouped)
    .filter(([_, errs]) => errs.length >= 3)
    .map(([type, errs]) => ({
      type,
      count: errs.length,
      trend: calculateTrend(errs), // 改善/恶化/持平
      recommendation: getRecommendation(type)
    }))

  return recurring
}
```

### 3.13 写作过程教学模块

**核心理念**：不只是给反馈，而是教方法。学生需要学会"怎么写"，而不只是知道"写得不好"。

**写作过程四阶段**：

| 阶段 | 教学内容 | 训练方式 |
|------|---------|---------|
| **审题** | 关键词提取、核心矛盾识别、多角度立意 | L1 审题立意专项 |
| **构思** | 角度选择、论据筛选、提纲编写 | L2 段落功能卡 + 思维训练 |
| **表达** | 段落展开、语言组织、修辞运用 | L3-L6 片段写作 |
| **修改** | 检查逻辑、调整结构、润色语言 | 训练后的修改环节 |

**审题教学**：

```
审题三步法：
1. 提取关键词：题目中的核心概念是什么？
2. 识别矛盾：题目在问什么？核心矛盾是什么？
3. 多角度思考：从个人/群体/社会/历史等不同角度，能得出什么立意？

示例：
题目："真正的勇气"
1. 关键词：勇气、真正
2. 矛盾：勇气 vs 鲁莽？勇气的表现形式？
3. 角度：
   - 个人：勇气是明知恐惧仍前行
   - 群体：勇气是集体行动中的担当
   - 历史：勇气在不同时代的含义
```

**构思教学**：

```
提纲编写四问：
1. 我的核心论点是什么？（一句话）
2. 我打算从哪几个方面论证？（分论点）
3. 每个分论点用什么论据支撑？（事例/名言/数据）
4. 段落之间如何衔接？（过渡句）

示例：
核心论点：真正的勇气是理性判断后的坚持
分论点1：勇气不是冲动，而是深思熟虑后的选择
分论点2：勇气需要智慧支撑，否则就是鲁莽
分论点3：真正的勇气往往不被理解，但依然坚持
```

**表达教学**：

```
论证段展开公式：
论点句 + 论据（事例/名言）+ 分析（为什么这个论据能证明论点）+ 小结

示例：
【论点句】真正的勇气，是理性判断后的坚持。
【论据】司马迁受宫刑后，没有选择以死明志，而是忍辱负重完成《史记》。
【分析】他的选择看似"懦弱"，实则是对人生价值的理性判断——他清楚地知道，
自己的使命是完成这部著作，而非逞一时之勇。
【小结】这种基于理性判断的坚持，才是真正的勇气。
```

**修改教学**：

```
修改四步法：
1. 逻辑检查：论点和论据是否匹配？论证是否有跳跃？
2. 结构检查：段落顺序是否合理？详略是否得当？
3. 语言检查：是否有口语化表达？句式是否单调？
4. 规范检查：错别字、标点、格式

优先级：逻辑 > 结构 > 语言 > 规范
（先保证说得对，再说得好）
```

### 3.14 文体意识训练模块

**核心理念**：不同文体的写作规则完全不同。学生需要建立"文体意识"，知道什么文体该写什么、不该写什么。

**语文文体分类**：

| 文体 | 核心特征 | 写作要求 | 高考出题频率 |
|------|---------|---------|-------------|
| 议论文 | 观点+论据+论证 | 论点明确，论据典型，论证严密 | 最高（70%+） |
| 记叙文 | 人物+事件+情感 | 叙事生动，人物鲜明，情感真挚 | 中等（20%） |
| 散文 | 形散神聚 | 意境优美，线索清晰，情感真实 | 较低（10%） |
| 应用文 | 格式规范 | 格式正确，内容简洁，语气得体 | 偶尔（书信/演讲稿） |

**英语文体分类**：

| 文体 | 核心特征 | 高考题型 |
|------|---------|---------|
| 应用文 | 格式规范（书信/通知/演讲稿） | 书信（最常见） |
| 议论文 | 观点+论据+结论 | 读后续写前的讨论 |
| 记叙文 | 人物+事件+情感 | 读后续写（新题型） |

**文体训练内容**：

| 文体 | 训练重点 | 对应层级 |
|------|---------|---------|
| 议论文 | 论点提炼、论据选择、论证方法 | L1-L4, L7 |
| 记叙文 | 叙事顺序、人物描写、情感表达 | 独立训练链 |
| 应用文 | 格式规范、语气把控、内容简洁 | L3（英语） |

**文体意识 Prompt**：
```
学生正在写一篇议论文。请检查：
1. 是否有明确的论点？
2. 论据是否能支撑论点？
3. 论证是否严密？
4. 是否有记叙文的倾向（叙事过多，论证不足）？

如果发现文体混淆，请指出并给出修正建议。
```

---

## 四、AI 介入原则

### 4.1 教练红线

| 场景 | 允许 | 禁止 |
|------|------|------|
| 审题立意 | 给出 3 个参考角度，分析优劣 | 直接告诉学生“你应该选这个” |
| 段落改写 | 标出 3 个问题，给出修改方向 | 直接输出改写好的完整段落 |
| 素材推荐 | 推荐 3 个素材，让学生判断哪个最贴切 | 直接把素材嵌入学生作文 |
| 句式仿写 | 给例句 + 规则说明，学生仿写 | 直接生成仿写结果 |
| 全文评审 | 聚焦最弱的 1-2 个维度给建议 | 一次性给 10 条泛泛建议 |

### 4.2 反馈语言风格

- **中文**：教师口吻，但具体、 actionable，避免空泛评价。
- **英文训练的分析/反馈**：暂时使用中文，降低学生理解成本。

### 4.3 反馈呈现策略

- **每次只给最优先的 1-2 条建议**，不要列表轰炸。
- **建议必须定位到具体位置**（原文高亮），学生点击后跳转到对应处。
- **提供“为什么”**：不只是说“这里过渡不好”，而是说“读者从 A 到 B 会困惑，因为你省略了 X”。

---

## 五、时间递进策略

### 5.1 语文时间递进

| 阶段 | 审题立意 | 微大纲 | 片段写作 | 全文写作 |
|------|---------|--------|---------|---------|
| 萌芽期 | 不限时 | 不限时 | 不限时 | 60 分钟 |
| 生长期 | 5 分钟 | 10 分钟 | 15 分钟 | 50 分钟 |
| 茁壮期 | 3 分钟 | 5 分钟 | 10 分钟 | 45 分钟 |

### 5.2 英语时间递进

| 阶段 | 句式仿写 | 段落写作 | 全文写作 |
|------|---------|---------|---------|
| 萌芽期 | 不限时 | 不限时 | 40 分钟 |
| 生长期 | 5 分钟 | 10 分钟 | 30 分钟 |
| 茁壮期 | 3 分钟 | 8 分钟 | 25 分钟 |

> 教师/学生可手动调整时间，系统默认按阶段标签设定。

---

## 五点五、年级专属训练模式

> 注意：本节编号为 5.5，因为内容与第五节（时间递进策略）紧密相关。后续章节将重新排序。

### 5.5.1 高一模式：基础夯实期

**核心理念**：时间充裕，重点是打好基础、养成习惯、积累素材。

**训练特点**：
- 八大模块均衡发展，不偏科
- 不急于全文训练，扎实练好每个能力点
- 重点建立个人素材库（好词好句、论据、事例）
- 间隔重复正常节奏，巩固为主

**首页展示**：
```
📚 今日学习（预计 10-15 分钟）
  ├── 语文 L3 开头段专项
  └── 英语 L2 段落骨架

📖 今日素材积累
  └── 推荐阅读：关于"坚持"的3个经典论据

📊 我的进度
  └── 语文 L1 ✓ L2 ✓ L3 ▶ | 英语 L1 ✓ L2 ▶
```

### 5.5.2 高二模式：针对性突破期

**核心理念**：开始聚焦，集中攻克薄弱点，增加全文训练比例。

**训练特点**：
- 重点强化薄弱能力点（系统自动识别）
- 增加全文训练比例（每周 1-2 次）
- 间隔重复稍紧凑，加快巩固节奏
- 开始接触考试策略（时间分配、审题技巧）

**首页展示**：
```
🎯 今日重点突破（预计 20-30 分钟）
  ├── 🔥 薄弱点：语文 L4 论证段（连续 2 次得分偏低）
  └── 📝 全文训练：英语 应用文（本周推荐）

📊 薄弱点追踪
  └── 语文论证段 ██████░░░░ 65分 → 目标 75分
```

### 5.5.3 高三模式：冲刺提分期

**核心理念**：时间紧迫，一切为了提分。只练最能提分的能力点，强化限时训练和考试策略。

**训练特点**：
- **冲刺路径**：系统根据能力诊断，直接推荐最能提分的 2-3 个能力点
- **全程限时**：所有训练都按考场时间压力执行
- **高频复习**：间隔重复紧凑，确保学过的不丢
- **模拟考试**：每周 1-2 次完整限时模拟
- **考试策略**：时间分配、审题策略、取舍判断
- **心理训练**：考场紧张、卡壳、时间不够时的应对

**首页展示**：
```
⚡ 今日冲刺（预计 40-60 分钟）
  ├── 🔄 复习：语文 L4 论证段（明日到期）
  ├── 📝 冲刺训练：英语 L5 语法纠错（最能提分）
  └── 🎯 限时模拟：语文 全文写作（45分钟）

📊 冲刺进度
  └── 距高考 XX 天 | 已完成冲刺训练 12 次 | 预计提分 +8-12 分

🧠 考试策略
  └── 本周重点：议论文时间分配（审题5min → 提纲10min → 写作30min）
```

### 5.5.4 年级专属功能模块

#### 冲刺模式（高三专属）

**功能**：
- 系统根据能力诊断，识别出"最能提分"的 2-3 个能力点
- 这些能力点的训练优先级最高，复习间隔最短
- 每周生成"冲刺报告"：本周练了什么、预计提分多少、下周重点

**提分预估算法**：
```
基于历史数据：
- 某能力点从 60 分提升到 75 分，平均需要 8 次训练
- 某能力点从 75 分提升到 85 分，平均需要 12 次训练
- 结合当前水平和距考试时间，预估可提升幅度
```

#### 同题对比（示范模仿的强化版）

**功能**：
- 学生写完后，系统自动找一篇同题高分范文
- 逐段并排对比，AI 指出关键差异
- 不是泛泛的"你的论证不够深入"，而是"同样写勇气，高分作文用了XX事例，你用了YY，差距在于..."

**对比维度**：
| 维度 | 对比内容 |
|------|---------|
| 立意 | 核心论点的深度和角度 |
| 结构 | 段落安排和逻辑推进 |
| 论据 | 事例的选择和运用方式 |
| 语言 | 句式变化和用词精准度 |
| 开头/结尾 | 引人入胜程度和升华高度 |

#### 限时模拟（考试实战训练）

**功能**：
- 完整的考场模拟：审题 → 提纲 → 写作 → 检查
- 严格计时，中途不能暂停
- 模拟后给出"考场表现报告"：时间分配是否合理、哪个环节耗时过长

**模拟流程**：
```
选择模拟题目（高考真题/模拟题）
  → 设置时间（语文 60 分钟 / 英语 35 分钟）
    → 倒计时开始
      → 审题阶段（5 分钟，系统提醒）
        → 提纲阶段（10 分钟，系统提醒）
          → 写作阶段（40 分钟，系统提醒）
            → 检查阶段（5 分钟，系统提醒）
              → 提交 → AI 评审 + 考场表现报告
```

#### 素材库（个人积累系统）

**功能**：
- 收藏好词好句、经典论据、典型事例
- 按主题/文体/能力点分类
- 写作时可快速检索相关素材
- AI 推荐："你收藏的这个素材可以用在当前题目中"

**素材来源**：
- 范文赏析中收藏的片段
- AI 推荐的素材
- 学生自己整理的素材

#### 修改示范（从"知道问题"到"学会修改"）

**功能**：
- AI 不只指出问题，还展示**具体怎么改**
- 修改前后对比，高亮修改处，附带修改理由
- 教授修改方法："当论证不够深入时，可以尝试追问'为什么'和'怎么样'"

**修改教学 Prompt**：
```
学生作文中存在以下问题：
{问题描述}

请：
1. 展示修改前后的对比（只修改 1-2 处关键问题）
2. 说明修改的理由："为什么这样改更好"
3. 教授修改方法："下次遇到类似问题，可以这样处理"
```

#### 写作心理训练（考场应对）

**功能**：
- 考场紧张时的应对策略
- 写到一半卡住时的破局方法
- 时间不够时的取舍判断

**场景化训练**：
| 场景 | 策略 |
|------|------|
| 审题抓不住核心 | 先写能确定的部分，回头再审 |
| 写到一半卡住 | 跳过卡壳处，先写后面，最后补 |
| 时间只剩 10 分钟 | 快速写结尾，确保结构完整 |
| 发现跑题了 | 不要重写，调整后续段落方向 |

#### 考试策略模块

**功能**：
- 时间分配建议（审题/提纲/写作/检查各占多少时间）
- 审题策略（如何快速抓住题目核心）
- 取舍判断（哪个部分详写、哪个略写）

**语文时间分配模板**：
```
总时间：60 分钟
├── 审题立意：5 分钟（8%）
├── 提纲编写：10 分钟（17%）
├── 正文写作：40 分钟（67%）
└── 检查修改：5 分钟（8%）
```

**英语时间分配模板**：
```
总时间：35 分钟
├── 审题：3 分钟（9%）
├── 提纲：5 分钟（14%）
├── 写作：25 分钟（71%）
└── 检查：2 分钟（6%）
```

#### 个人弱项档案

**功能**：
- 系统追踪学生的常见问题，形成个人弱项档案
- 每次训练后更新弱项档案
- 针对弱项推荐专项训练

**弱项档案内容**：
```json
{
  "userId": "user-001",
  "weakPoints": [
    {
      "type": "论证逻辑",
      "description": "论据与论点脱节",
      "frequency": 5,
      "lastOccurrence": "2026-05-20",
      "improvement": "从65分提升到72分",
      "recommendedTraining": "L4-论证段专项"
    },
    {
      "type": "语言表达",
      "description": "句式单一，缺乏变化",
      "frequency": 3,
      "lastOccurrence": "2026-05-18",
      "improvement": "待提升",
      "recommendedTraining": "L1-句式仿写"
    }
  ]
}
```

---

## 六、中英分轨设计要点

| 维度 | 语文 | 英语 |
|------|------|------|
| 核心痛点 | 立意深度、素材陈旧、结构套路化 | 句式单一、词汇贫乏、语法错误 |
| 训练重心 | 思 → 结构 → 语言（先想清再写好） | 形 → 段 → 篇（先模仿再创造） |
| AI 角色 | 苏格拉底式对话者 | 语法检查器 + 句式教练 |
| 反馈焦点 | “你为什么这样想？” | “这里违反了什么规则？如何修正？” |
| 评分侧重点 | 切题度 > 深度 > 结构 > 语言 | 语法准确性 > 句式多样性 > 词汇 > 内容 |

---

## 七、现有 Demo 的演进路径

当前 Demo 已实现“选题 → 写作 → AI 评审 → 看反馈”，但它是**以整篇作文为单元**的。演进目标是将它拆分为**以训练单元为核心**的新架构。

### 7.1 第一步：接入预评分（新用户必经之路）

新增诊断流程：
```
注册/登录 → 预评分（10-15 分钟）→ 能力雷达图 + 推荐起始层级 → 进入训练首页
```

### 7.2 第二步：重构训练首页

当前首页是“选题列表”。新首页是：
```
今日推荐训练（根据阶段标签智能推荐 1-2 个单元）
  ├── 语文：L3 开头段专项（你上次得分 72，建议再练一次）
  └── 英语：L2 段落骨架（新解锁，尝试一下？）

我的训练进度
  ├── 语文：L1 ✓ L2 ✓ L3 ▶ L4 🔒（推荐）...
  └── 英语：L1 ✓ L2 ▶ L3 🔒...

能力雷达图（更新于 X 天前，点击重新诊断）
```

> 注意：🔒 只是“推荐等待”，点击仍可进入，不设硬锁。

### 7.3 第三步：按层级改造编辑器

不同层级的编辑器形态不同：

- **L1 审题立意**：表单式输入（关键词 + 一句话论点），不是富文本编辑器
- **L2 段落功能卡**：5 个输入框，每框限制 30 字
- **L3-L6 片段写作**：简化版编辑器，只写一段，字数目标明确
- **L7/L6 全文写作**：当前 Demo 的编辑器形态，保留

### 7.4 第四步：改造 AI 评审

评审 Prompt 不再按”整篇作文”设计，而是**按训练单元目标设计**：

- L3 开头段评审 Prompt：只评”是否引人入胜”和”是否点明论点”
- L4 论证段评审 Prompt：只评”论点-论据-分析”闭环是否完整
- 每个层级的评分维度、满分值、通过建议都不同

### 7.5 第五步：接入四大支柱

将阅读输入、思维训练、复盘反思、示范模仿整合进训练流程：

**5a. 阅读输入模块**
- 范文片段库：按能力点（开头段/论证段/过渡段/结尾段）分类存储
- 训练前推送：学生进入训练时，可选”先看范文”
- 赏析引导 Prompt：AI 引导学生分析范文的结构、修辞、论证手法

**5b. 思维训练模块**
- 独立训练入口：首页增加”思维训练”板块
- 训练类型：审题立意、段落排序、论证链条、提纲编写、多角度分析
- 与训练链联动：训练链中遇到困难时，系统推荐回到思维训练巩固

**5c. 复盘反思模块**
- 训练报告：每次训练后自动生成，包含总结、反馈、薄弱点、推荐
- 阶段性回顾：每周/每阶段生成能力曲线对比、训练量统计、进步亮点
- 薄弱点追踪：连续 3 次某维度得分低于阈值，自动标记并推荐复习

**5d. 示范模仿模块**
- 修改前后对比：AI 生成修改建议版，展示 diff
- 范文对比：学生作文 vs 同题高分范文，并排展示
- 典型错误案例：系统维护错误库，犯错时推送相关案例

### 7.6 第六步：接入年级专属功能

根据年级差异化训练策略：

**6a. 高一模式：基础夯实期**
- 八大模块均衡发展，不急于全文训练
- 重点建立个人素材库
- 间隔重复正常节奏

**6b. 高二模式：针对性突破期**
- 重点强化薄弱能力点（系统自动识别）
- 增加全文训练比例
- 间隔重复稍紧凑

**6c. 高三模式：冲刺提分期**
- 冲刺路径：系统推荐最能提分的 2-3 个能力点
- 全程限时：所有训练都按考场时间压力执行
- 高频复习：间隔重复紧凑
- 模拟考试：每周 1-2 次完整限时模拟
- 考试策略：时间分配、审题技巧、取舍判断
- 心理训练：考场紧张、卡壳、时间不够时的应对

**6d. 通用功能模块**
- 同题对比：学生作文 vs 同题高分范文，逐段对比
- 限时模拟：完整考场模拟，严格计时
- 素材库：个人积累的好词好句、论据、事例
- 修改示范：AI 不只指出问题，还展示具体怎么改
- 写作心理：考场紧张、卡壳、时间不够时的应对策略
- 考试策略：时间分配、审题技巧、取舍判断
- 个人弱项档案：系统追踪学生的常见问题，形成个人弱项档案

### 7.7 第七步：接入教学支撑模块

将评分标准、错误类型学、写作过程教学、文体意识训练整合进系统：

**7a. 评分标准显式化**
- 每次训练前展示本次训练的评分标准
- AI 用通俗语言解读标准要求
- 评审时明确指出"你在哪个维度达到了什么等级"

**7b. 错误类型学**
- 建立语文/英语的错误分类体系
- AI 评审时自动分类错误类型
- 形成个人错误档案，识别反复出现的错误模式

**7c. 写作过程教学**
- 审题三步法：提取关键词 → 识别矛盾 → 多角度思考
- 构思四问：核心论点 → 分论点 → 论据 → 衔接
- 表达公式：论点句 + 论据 + 分析 + 小结
- 修改四步法：逻辑检查 → 结构检查 → 语言检查 → 规范检查

**7d. 文体意识训练**
- 语文：议论文/记叙文/散文/应用文的写作规则
- 英语：应用文/议论文/记叙文的写作规则
- AI 评审时检查文体一致性，防止文体混淆

---

## 八、近期不做的事（明确边界）

以下功能在核心阶段不实现，避免稀释焦点：

- ❌ 教师管理端（班级管理、作业系统、学情分析、教师仪表盘）
- ❌ 家长端
- ❌ 词汇本（词汇卡片式记忆）
- ❌ 同伴互评
- ❌ 语音输入 / 小程序
- ❌ 多模型路由降本

> 注意：
> - "教师管理端"指教师用于管理班级和学生的管理界面，不包括教学理念和评分标准。
> - 本系统已融入老师视角的教学支撑模块（评分标准显式化、错误类型学、写作过程教学、文体意识训练），这些是教学方法论，不是教师管理工具。
> - 间隔重复、限时模拟、考试策略、写作心理训练等已在核心功能中实现。
> - 以上功能在 PLAN.md 中有设计，待核心训练闭环跑通后再考虑。

---

## 九、关键决策（已确定）

### 9.1 题库来源

- **作文题目**：AI 搜集近年高考真题 + 各地模拟题，语文和英语分别建立题库，越多越好。
- **优秀范文**：同步搜集对应题目下的高分范文，作为训练中的对比参照。
- **动态生成**：以搜集的真题/模拟题为基础，AI 可基于母题进行变体生成（换情境、换文体要求、换限定词），保证题库的持续扩充。

### 9.2 评审策略

- **每次评审都重新调用 AI**，不缓存旧结果。
- **每次评审结果必须落库**，保留完整历史记录，支持学生随时回溯查看。
- 同一道题反复练时，AI 会基于学生当前作答重新评审，自然体现进步。

### 9.3 能力画像存储

- **四维度历史曲线**：内容、结构、语言、规范。
- 每次训练（包括片段训练和全文训练）的评分都按这 4 个维度记录，形成时间序列。
- 学生在训练首页看到的雷达图和折线图，就是这 4 个维度的历史趋势。
- 预评分的 5 个微任务（方向感/结构感/语言精度/句式能力/规范意识）仅在诊断阶段使用，不进入长期曲线。

---

## 十、数据需求清单

### 10.1 需要搜集/准备的数据

| 数据类型 | 语文 | 英语 | 说明 |
|---------|------|------|------|
| 高考真题 | 2015-2026 | 2015-2026 | 包含题目文本、文体要求 |
| 模拟题 | 越多越好 | 越多越好 | 各省模拟、名校模拟 |
| 高分范文 | 每题 2-3 篇 | 每题 2-3 篇 | 用于对比、拆解、仿写 |
| 范文片段库 | 按能力点分类 | 按能力点分类 | 开头段/论证段/过渡段/结尾段精选片段，用于阅读输入模块 |
| 母句式（英语） | — | 50-100 个 | 倒装/强调/分词/虚拟/定从嵌套等 |
| 诊断题库 | 30-50 题 | 30-50 题 | 预评分 5 个微任务专用 |
| 思维训练题库 | 30-50 题 | 30-50 题 | 审题/排序/论证链条/提纲专项训练 |
| 典型错误案例库 | 50-100 例 | 50-100 例 | 常见错误 + 修改方法，用于示范模仿模块 |
| 间隔重复题库 | 20-30 题 | 20-30 题 | 同能力点变体题，用于间隔复习 |
| 自评对比案例 | 30-50 例 | 30-50 例 | 学生自评 vs AI 评估的对比案例 |
| 评分标准库 | 4-6 套 | 4-6 套 | 不同文体、不同等级的评分标准 |
| 错误类型库 | 30-50 类 | 30-50 类 | 常见错误分类 + 典型案例 |
| 写作过程教学案例 | 20-30 例 | 20-30 例 | 审题/构思/表达/修改的教学案例 |
| 文体训练题库 | 20-30 题 | 20-30 题 | 不同文体的专项训练题 |
| 考试策略库 | 20-30 条 | 20-30 条 | 时间分配、审题技巧、取舍判断策略 |
| 心理训练场景 | 10-20 个 | 10-20 个 | 考场紧张、卡壳、时间不够等场景的应对策略 |
| 冲刺提分数据 | 50-100 条 | 50-100 条 | 各能力点提分所需训练次数和时间的数据 |

### 10.2 数据格式

每道作文题统一格式：
```json
{
  "id": "cn-2024-1",
  "source": "高考真题",
  "year": 2024,
  "region": "全国甲卷",
  "subject": "chinese",
  "type": "议论文",
  "title": "真正的勇气",
  "description": "题目完整描述...",
  "requirements": "不少于800字...",
  "sampleEssays": [
    { "score": 58, "content": "...", "highlights": ["..."] }
  ],
  "tags": ["勇气", "个人成长", "思辨"]
}
```

---

## 十一、交互设计决策

### 11.1 训练后的修改环节

**方案**：当场修改 → AI 再评（推荐但不强制）

**流程**：
```
学生完成训练 → AI 给出反馈 → 系统提示"建议根据反馈修改后再提交" →
  学生选择【修改】或【跳过】 →
    若修改：AI 重点评审"是否改对了上次指出的问题"，给出【进步分】 → 学生可再次修改或结束
    若跳过：直接进入下一题/层级
```

**进步分设计**：
- 第一次评审：基础分（如 65 分）
- 修改后再评：进步分（如 +8 分），AI 明确说明"你改对了 X 和 Y，但 Z 还没改到位"
- 进步分单独记录，与基础分一起进入能力曲线

**原则**：鼓励修改闭环，但不强制。学生自主决定何时进入下一题。

### 11.2 首页语文/英语组织方式

**方案**：并列展示，学生自选；根据年级调整首页布局

**高一/高二首页布局**：
```
今日推荐训练
  ├── 🔄 复习（到期）
  │   └── 语文 L4 论证段（上次得分 72，建议复习）
  ├── 📝 新训练
  │   ├── 语文 L3 开头段专项（上次得分 68，建议再练一次）
  │   └── 英语 L2 段落骨架（新解锁，尝试一下？）
  └── ⚡ 薄弱点强化
      └── 英语 L1 句式仿写（连续 2 次得分偏低）

我的训练进度
  ├── 语文：L1 ✓  L2 ✓  L3 ▶  L4 ...
  └── 英语：L1 ✓  L2 ▶  L3 ...

能力雷达图（语文 | 英语 切换）

我的成长轨迹
  └── 连续训练 5 天 🔥 | 本月训练 12 次 | 解锁成就 3 个
```

**高三首页布局（冲刺模式）**：
```
⚡ 今日冲刺（距高考 XX 天）
  ├── 🔄 复习：语文 L4 论证段（明日到期，优先复习）
  ├── 📝 冲刺训练：英语 L5 语法纠错（最能提分 +3-5 分）
  └── 🎯 限时模拟：语文 全文写作（45 分钟，本周推荐）

📊 冲刺进度
  └── 已完成冲刺训练 12 次 | 预计提分 +8-12 分

🧠 考试策略
  └── 本周重点：议论文时间分配（审题 5min → 提纲 10min → 写作 30min）

我的训练进度
  ├── 语文：L1 ✓  L2 ✓  L3 ✓  L4 ▶  L5 ...
  └── 英语：L1 ✓  L2 ✓  L3 ✓  L4 ▶  L5 ...

能力雷达图（语文 | 英语 切换）
```

**原则**：
- 高一/高二侧重"今日学习"，鼓励均衡发展
- 高三侧重"今日冲刺"，聚焦提分效率
- 语文和英语平等并列，学生点击哪个就进入哪个的训练流程

### 11.3 Diff 对比（核心功能）

**定位**：Diff 不是附加功能，而是"修改→进步"闭环的数据基础。必须在核心阶段实现。

**应用场景**：

1. **学生修改后确认**：学生根据 AI 反馈修改完，系统先展示 diff，让学生有意识地审视自己的修改
2. **AI 再评审输入**：AI 再评审时直接看到 diff，聚焦"修改是否解决了上次的问题"
3. **建议状态追踪**：每条 AI 建议标记为"已解决/未解决/改偏了/新增问题"

**Diff 展示方式**：
- 段级 diff（不是字符级），因为作文按段落组织
- 删除 → 红色删除线
- 新增 → 绿色高亮
- 修改 → 左侧红色原文 + 右侧绿色改文

**建议状态标签**：

| 状态 | 含义 | 系统如何判定 |
|------|------|-------------|
| 已解决 | 学生根据建议修改了对应位置 | diff 中该位置有变动，AI 确认改动正确 |
| 未解决 | 学生没改 | diff 中该位置无变动，AI 再次指出 |
| 改偏了 | 学生改了，但没改对 | diff 中有变动，但 AI 认为方向错了 |
| 新增问题 | 上次没指出，但这次出现了 | diff 中新增内容引入了问题 |

**原则**：让反馈从"静态列表"变成"可追踪的任务清单"。

---

## 十二、技术决策

### 12.1 数据库选型

**核心功能开发阶段：SQLite**

| 阶段 | 数据库 | 理由 |
|------|--------|------|
| 核心功能开发 | SQLite | 零配置、文件存储、Prisma 完美支持、开发体验最佳 |
| 扩展功能/上线 | PostgreSQL (Supabase) | 支持并发、Serverless 部署、Auth、实时订阅 |

**迁移成本**：Prisma 切换 provider 只需改一行代码（`provider = "sqlite"` → `provider = "postgresql"`），schema 本身不用改。

**注意**：SQLite 不适用于 Vercel 等 Serverless 部署环境（文件不持久化）。如需在线演示，在部署前必须切到 PostgreSQL。

### 12.2 移动端适配

高中生主要使用手机，必须移动优先设计：

**设计原则**：
- 移动优先：所有界面先设计手机版，再适配桌面版
- 单手操作：核心操作（开始训练、提交、查看反馈）支持单手完成
- 离线缓存：范文片段、评分标准、错误案例库支持离线访问
- 流式加载：AI 反馈实时流式显示，避免长时间等待

**关键页面移动端适配**：

| 页面 | 移动端适配要点 |
|------|--------------|
| 首页 | 卡片式布局，上下滑动，今日推荐置顶 |
| 训练页 | 全屏编辑器，隐藏导航栏，专注写作 |
| AI 反馈 | 流式显示，关键建议置顶，可折叠详情 |
| Diff 对比 | 段级对比，左右滑动查看原文/改文 |
| 能力雷达图 | 简化版，只显示关键维度 |
| 素材库 | 列表式，支持搜索和标签筛选 |

### 12.3 数据隐私与安全

学生数据（作文、成绩、弱项档案）是敏感数据，需要保护：

**数据分级**：

| 数据类型 | 敏感度 | 存储策略 |
|---------|--------|---------|
| 用户信息（姓名、年级） | 中 | 加密存储，最小化采集 |
| 作文内容 | 高 | 本地优先，同步时加密 |
| 成绩和能力画像 | 高 | 加密存储，仅本人可见 |
| 弱项档案 | 高 | 加密存储，仅本人可见 |
| 训练记录 | 中 | 脱敏后可用于产品改进 |

**隐私保护措施**：
- 不收集不必要的个人信息
- 作文内容默认本地存储，仅在需要 AI 评审时上传
- 评审完成后，原始作文内容可选择删除
- 不向第三方分享学生数据
- 支持学生导出和删除自己的数据

### 12.5 Prisma 数据库模型

```prisma
// 用户表
model User {
  id            String    @id @default(cuid())
  name          String
  grade         String    // '高一' | '高二' | '高三'
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  // 关联
  abilityProfiles AbilityProfile[]
  trainings       Training[]
  reviewSchedules ReviewSchedule[]
  materials       Material[]
  errorRecords    ErrorRecord[]
  achievements    Achievement[]
  weakPoints      WeakPoint[]
}

// 能力画像表
model AbilityProfile {
  id            String    @id @default(cuid())
  userId        String
  subject       String    // 'chinese' | 'english'
  stage         String    // '萌芽期' | '生长期' | '茁壮期'
  startLevel    Int       // 推荐起始层级
  dimensions    String    // JSON: 5个维度得分
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  user          User      @relation(fields: [userId], references: [id])
}

// 训练记录表
model Training {
  id            String    @id @default(cuid())
  userId        String
  subject       String
  level         String    // 'L1-审题立意' | 'L2-段落功能卡' | ...
  topic         String    // 题目
  content       String    // 学生作文内容
  genre         String?   // 文体
  score         Int?      // 基础分
  improvementScore Int?   // 进步分
  dimensions    String?   // JSON: 4维度得分
  aiFeedback    String?   // JSON: AI反馈
  errorTypes    String?   // JSON: 错误分类
  selfEval      String?   // JSON: 自评数据
  timeSpent     Int?      // 耗时（秒）
  isSimulated   Boolean   @default(false) // 是否限时模拟
  createdAt     DateTime  @default(now())

  user          User      @relation(fields: [userId], references: [id])
  reviewSchedules ReviewSchedule[]
  errorRecords    ErrorRecord[]
}

// 间隔重复调度表
model ReviewSchedule {
  id            String    @id @default(cuid())
  userId        String
  trainingId    String?
  abilityPoint  String    // 'L4-论证段'
  subject       String
  lastPracticed DateTime
  lastScore     Int
  reviewCount   Int       @default(0)
  nextReview    DateTime
  status        String    // 'new' | 'learning' | 'reviewing' | 'mastered'
  interval      Int       // 当前间隔（天）

  user          User      @relation(fields: [userId], references: [id])
  training      Training? @relation(fields: [trainingId], references: [id])
}

// 素材库表
model Material {
  id            String    @id @default(cuid())
  userId        String
  content       String
  source        String    // '范文' | 'AI推荐' | '学生整理'
  tags          String    // JSON: 主题标签
  category      String    // '论据' | '名言' | '事例' | '好词好句'
  subject       String
  usageCount    Int       @default(0)
  createdAt     DateTime  @default(now())

  user          User      @relation(fields: [userId], references: [id])
}

// 错误记录表
model ErrorRecord {
  id            String    @id @default(cuid())
  userId        String
  trainingId    String
  errorType     String    // '逻辑类-偷换概念'
  location      String    // '第三段第二句'
  originalText  String
  explanation   String
  severity      String    // 'high' | 'medium' | 'low'
  isRecurring   Boolean   @default(false)
  firstOccurrence DateTime
  lastOccurrence DateTime
  count         Int       @default(1)

  user          User      @relation(fields: [userId], references: [id])
  training      Training  @relation(fields: [trainingId], references: [id])
}

// 成就表
model Achievement {
  id            String    @id @default(cuid())
  userId        String
  type          String    // 'first_training' | 'streak_7' | ...
  name          String    // '初试锋芒'
  description   String
  unlockedAt    DateTime  @default(now())

  user          User      @relation(fields: [userId], references: [id])
}

// 弱项档案表
model WeakPoint {
  id            String    @id @default(cuid())
  userId        String
  type          String    // '论证逻辑' | '语言表达'
  description   String
  frequency     Int       @default(1)
  lastOccurrence DateTime
  improvement   String?   // '从65分提升到72分'
  recommendedTraining String? // 'L4-论证段专项'

  user          User      @relation(fields: [userId], references: [id])
}
```

---

*最后更新：2026-05-23*

---

## 十三、十二大模块实现要点

### 13.1 阅读输入的技术实现

**数据存储**：
- 范文片段按 `{能力点, 主题, 文体}` 三维标签分类
- 每个片段附带 `{赏析要点, 修辞手法, 论证方法}` 元数据
- 支持学生收藏，形成个人素材库

**AI 推荐逻辑**：
- 根据学生当前训练层级，推送同层级的范文片段
- 优先推送与学生薄弱点相关的片段（如论证弱，推论证段范文）
- 每次推送 2-3 段，避免信息过载

**赏析引导 Prompt 模板**：
```
你是一位语文/英语教师。请分析以下范文片段，引导学生理解：
1. 这段文字的核心手法是什么？
2. 为什么这样写效果好？
3. 学生可以在自己的写作中如何借鉴？

注意：不要直接告诉答案，而是通过提问引导学生思考。
```

### 13.2 思维训练的技术实现

**题库结构**：
```json
{
  "id": "think-cn-001",
  "type": "审题立意",
  "subject": "chinese",
  "difficulty": "medium",
  "prompt": "以'真正的勇气'为题...",
  "referenceAngles": [
    {"angle": "勇气是明知恐惧仍前行", "quality": "深刻"},
    {"angle": "勇气不是鲁莽", "quality": "辩证"}
  ],
  "commonMistakes": ["只写个人经历，缺乏思辨"]
}
```

**评分逻辑**：
- 审题立意：AI 判断是否抓住核心矛盾、是否跑题、是否有深度
- 段落排序：系统预设正确答案，学生排序后自动判分
- 论证链条：AI 判断逻辑是否闭环、是否有跳跃
- 提纲编写：AI 评估结构完整性、段落功能清晰度

### 13.3 复盘反思的技术实现

**训练报告生成 Prompt**：
```
你是一位高中语文/英语教师。请根据以下训练数据生成训练报告：

训练层级：{层级名称}
学生得分：{4维度得分}
AI 反馈：{反馈内容}
学生修改情况：{修改状态}

请生成包含以下内容的报告：
1. 本次训练总结（1-2句话）
2. 最关键的改进方向（1条）
3. 与上次训练对比（如有历史数据）
4. 下次推荐训练（基于当前水平）

语气：鼓励性、具体、 actionable。
```

**薄弱点识别算法**：
```
if 连续3次某维度得分 < 阶段阈值:
    标记为"薄弱"
    推荐复习对应层级
elif 连续2次得分提升 > 5分:
    标记为"进步中"
    鼓励继续当前方向
```

### 13.4 示范模仿的技术实现

**修改建议版生成**：
- AI 只修改最需要改进的 1-2 处，不全面重写
- 修改处用高亮标记，附带修改理由
- 生成 diff 视图，让学生看到具体变化

**范文对比逻辑**：
- 找到同题高分范文，按段落对齐
- AI 指出关键差异：立意深度、论证手法、语言表达
- 引导学生自己发现差距，而非直接告诉答案

**典型错误案例库**：
```json
{
  "id": "err-cn-001",
  "type": "论证逻辑",
  "subject": "chinese",
  "errorExample": "勇气就是勇敢，所以我们需要勇气。",
  "correctedExample": "勇气不是鲁莽行事，而是明知恐惧仍选择前行。",
  "explanation": "犯了同义反复的逻辑错误，论证没有推进",
  "relatedLevel": "L4-论证段专项"
}
```

### 13.5 间隔重复的技术实现

**数据模型**：
```json
{
  "id": "review-001",
  "userId": "user-001",
  "abilityPoint": "L4-论证段",
  "subject": "chinese",
  "lastPracticed": "2026-05-20T10:00:00Z",
  "lastScore": 72,
  "reviewCount": 2,
  "nextReview": "2026-05-27T10:00:00Z",
  "status": "reviewing",
  "interval": 7
}
```

**调度算法**：
```typescript
function calculateNextReview(reviewCount: number, score: number): number {
  // 基础间隔（天）
  const baseIntervals = [1, 3, 7, 14, 30]
  const baseInterval = baseIntervals[Math.min(reviewCount, 4)]

  // 根据得分调整：分数高则间隔拉长，分数低则间隔缩短
  const scoreFactor = score >= 80 ? 1.5 : score >= 60 ? 1.0 : 0.7

  return Math.round(baseInterval * scoreFactor)
}
```

**每日检查逻辑**：
```typescript
async function getTodayReviewTasks(userId: string) {
  const today = new Date()
  const tasks = await prisma.reviewSchedule.findMany({
    where: {
      userId,
      nextReview: { lte: today },
      status: { not: 'mastered' }
    },
    orderBy: { nextReview: 'asc' },
    take: 3  // 每天最多推荐 3 个复习任务
  })
  return tasks
}
```

### 13.6 自我评估的技术实现

**自评数据模型**：
```json
{
  "id": "self-eval-001",
  "trainingId": "training-001",
  "userId": "user-001",
  "selfEvalIssues": [
    {"location": "第三段", "issue": "论证不够深入", "severity": "high"},
    {"location": "开头", "issue": "引入太慢", "severity": "medium"}
  ],
  "aiEvalIssues": [
    {"location": "第三段", "issue": "论据与论点脱节", "severity": "high"},
    {"location": "结尾", "issue": "缺乏升华", "severity": "high"}
  ],
  "accuracyScore": 0.5,
  "coverageScore": 0.33,
  "timestamp": "2026-05-23T10:00:00Z"
}
```

**准确率计算**：
```typescript
function calculateSelfEvalScore(selfEval: Issue[], aiEval: Issue[]): EvalResult {
  // 准确率：学生标注的问题中有多少是 AI 也认为是问题的
  const matched = selfEval.filter(s =>
    aiEval.some(a => a.location === s.location || a.issue === s.issue)
  )
  const accuracy = matched.length / selfEval.length

  // 覆盖度：AI 标注的问题中有多少被学生发现了
  const coverage = matched.length / aiEval.length

  // 优先级准确率：学生认为最严重的问题是否与 AI 判断一致
  const topSelf = selfEval.filter(s => s.severity === 'high')
  const topAi = aiEval.filter(a => a.severity === 'high')
  const priorityAccuracy = topSelf.filter(s =>
    topAi.some(a => a.location === s.location)
  ).length / topAi.length

  return { accuracy, coverage, priorityAccuracy }
}
```

**AI 对比分析 Prompt**：
```
学生在写作后先进行了自我评估，标注了以下问题：
{selfEvalIssues}

你的评审结果是：
{aiEvalIssues}

请分析：
1. 学生标注的问题中，哪些是准确的？
2. 学生漏掉了哪些重要问题？
3. 给出具体的改进建议，帮助学生提高自我诊断能力。

语气：鼓励性、具体。
```

### 13.7 动机维持的技术实现

**连续训练追踪**：
```typescript
interface StreakData {
  userId: string
  currentStreak: number      // 当前连续天数
  longestStreak: number      // 历史最长连续天数
  lastPracticedDate: string  // 上次训练日期
  totalTrainingDays: number  // 总训练天数
}

function updateStreak(data: StreakData, today: string): StreakData {
  const yesterday = getYesterday(today)

  if (data.lastPracticedDate === today) {
    // 今天已训练，不变
    return data
  }

  if (data.lastPracticedDate === yesterday) {
    // 昨天训练了，连续天数 +1
    return {
      ...data,
      currentStreak: data.currentStreak + 1,
      longestStreak: Math.max(data.longestStreak, data.currentStreak + 1),
      lastPracticedDate: today,
      totalTrainingDays: data.totalTrainingDays + 1
    }
  }

  // 断了，重新开始
  return {
    ...data,
    currentStreak: 1,
    lastPracticedDate: today,
    totalTrainingDays: data.totalTrainingDays + 1
  }
}
```

**成就系统数据模型**：
```json
{
  "id": "achievement-001",
  "userId": "user-001",
  "type": "first_training",
  "name": "初试锋芒",
  "description": "完成首次训练",
  "unlockedAt": "2026-05-23T10:00:00Z",
  "icon": "🎉"
}
```

**成就检查逻辑**：
```typescript
const ACHIEVEMENTS = [
  {
    type: 'first_training',
    name: '初试锋芒',
    description: '完成首次训练',
    check: (stats: UserStats) => stats.totalTrainings >= 1
  },
  {
    type: 'streak_7',
    name: '坚持不懈',
    description: '连续训练 7 天',
    check: (stats: UserStats) => stats.currentStreak >= 7
  },
  {
    type: 'monthly_star',
    name: '月度之星',
    description: '单月训练 ≥ 20 次',
    check: (stats: UserStats) => stats.monthlyTrainings >= 20
  },
  {
    type: 'first_mastery',
    name: '首次达标',
    description: '某能力点首次 ≥ 80 分',
    check: (stats: UserStats) => stats.hasAnyMastery
  }
]

async function checkAchievements(userId: string) {
  const stats = await getUserStats(userId)
  const existing = await getUnlockedAchievements(userId)
  const newAchievements = []

  for (const achievement of ACHIEVEMENTS) {
    if (!existing.includes(achievement.type) && achievement.check(stats)) {
      newAchievements.push(achievement)
      await unlockAchievement(userId, achievement)
    }
  }

  return newAchievements
}
```

**首页推荐逻辑**：
```typescript
async function getTodayRecommendations(userId: string) {
  const recommendations = []

  // 1. 到期的复习任务（优先级最高）
  const reviewTasks = await getTodayReviewTasks(userId)
  if (reviewTasks.length > 0) {
    recommendations.push({
      type: 'review',
      tasks: reviewTasks,
      priority: 1
    })
  }

  // 2. 推荐的新训练（根据能力水平）
  const newTrainings = await getRecommendedNewTrainings(userId)
  if (newTrainings.length > 0) {
    recommendations.push({
      type: 'new',
      tasks: newTrainings,
      priority: 2
    })
  }

  // 3. 薄弱点强化
  const weakPoints = await getWeakPoints(userId)
  if (weakPoints.length > 0) {
    recommendations.push({
      type: 'weak_point',
      tasks: weakPoints,
      priority: 3
    })
  }

  return recommendations
}
```

### 13.8 年级专属功能的技术实现

#### 冲刺模式实现

**冲刺路径生成**：
```typescript
async function generateSprintPath(userId: string) {
  const abilityProfile = await getAbilityProfile(userId)
  const grade = await getUserGrade(userId)
  const examDate = await getExamDate(userId)

  if (grade !== '高三') return null

  // 识别最能提分的 2-3 个能力点
  const weakPoints = identifyWeakPoints(abilityProfile)
  const sprintPoints = weakPoints
    .sort((a, b) => estimateImprovementPotential(a, b))
    .slice(0, 3)

  // 计算提分预估
  const estimatedImprovement = sprintPoints.reduce((sum, point) => {
    return sum + estimatePointImprovement(point, examDate)
  }, 0)

  return {
    sprintPoints,
    estimatedImprovement,
    weeklyGoal: generateWeeklyGoal(sprintPoints)
  }
}
```

**提分预估算法**：
```typescript
function estimatePointImprovement(point: AbilityPoint, examDate: Date): number {
  const daysUntilExam = daysBetween(new Date(), examDate)
  const currentScore = point.score
  const targetScore = Math.min(currentScore + 15, 85) // 上限 85 分

  // 基于历史数据：每提升 1 分需要的训练次数
  const trainingNeeded = (targetScore - currentScore) * 0.8

  // 可用训练时间
  const availableDays = Math.floor(daysUntilExam / 2) // 隔天训练
  const maxTrainings = availableDays * 2 // 每天 2 次训练

  // 实际可提升分数
  const achievableImprovement = Math.min(
    targetScore - currentScore,
    Math.floor(maxTrainings / trainingNeeded * (targetScore - currentScore))
  )

  return achievableImprovement
}
```

#### 同题对比实现

**对比数据结构**：
```typescript
interface ComparisonResult {
  studentEssay: string
  highScoreEssay: string
  paragraphComparisons: ParagraphComparison[]
  overallAnalysis: string
}

interface ParagraphComparison {
  studentParagraph: string
  modelParagraph: string
  differences: Difference[]
  aiComment: string
}

interface Difference {
  type: '立意' | '结构' | '论据' | '语言'
  description: string
  suggestion: string
}
```

**AI 对比 Prompt**：
```
请对比以下两篇作文：

学生作文：
{studentEssay}

高分范文（{score}分）：
{highScoreEssay}

请按段落对比分析：
1. 每段的核心差异是什么？
2. 高分范文好在哪里？
3. 学生可以如何借鉴？

注意：不要直接告诉答案，而是引导学生自己发现差距。
```

#### 限时模拟实现

**模拟流程控制**：
```typescript
interface SimulationConfig {
  totalTime: number  // 总时间（分钟）
  stages: Stage[]
}

interface Stage {
  name: string
  duration: number  // 该阶段时长（分钟）
  reminder: string  // 提醒内容
}

const CHINESE_SIMULATION: SimulationConfig = {
  totalTime: 60,
  stages: [
    { name: '审题立意', duration: 5, reminder: '请快速确定核心论点' },
    { name: '提纲编写', duration: 10, reminder: '请列出段落结构' },
    { name: '正文写作', duration: 40, reminder: '请开始写作' },
    { name: '检查修改', duration: 5, reminder: '请检查错别字和逻辑' }
  ]
}
```

**模拟报告生成**：
```typescript
function generateSimulationReport(result: SimulationResult): string {
  const timeAnalysis = analyzeTimeUsage(result.stageTimes)
  const contentAnalysis = analyzeContent(result.essay)
  const strategyAdvice = generateStrategyAdvice(timeAnalysis, contentAnalysis)

  return `
## 考场表现报告

### 时间分配
${timeAnalysis}

### 内容质量
${contentAnalysis}

### 策略建议
${strategyAdvice}
  `
}
```

#### 素材库实现

**素材数据模型**：
```typescript
interface Material {
  id: string
  userId: string
  content: string
  source: '范文' | 'AI推荐' | '学生整理'
  tags: string[]  // 主题标签
  category: string  // 分类：论据/名言/事例/好词好句
  subject: 'chinese' | 'english'
  usageCount: number
  createdAt: Date
}
```

**素材推荐逻辑**：
```typescript
async function recommendMaterials(userId: string, topic: string): Promise<Material[]> {
  const userMaterials = await getUserMaterials(userId, topic)

  // 优先推荐学生收藏的素材
  if (userMaterials.length > 0) {
    return userMaterials.slice(0, 3)
  }

  // 否则推荐系统素材
  return getSystemMaterials(topic, 3)
}
```

#### 修改示范实现

**修改教学 Prompt**：
```
学生作文中存在以下问题：
{problemDescription}

请：
1. 展示修改前后的对比（只修改 1-2 处关键问题）
2. 说明修改的理由："为什么这样改更好"
3. 教授修改方法："下次遇到类似问题，可以这样处理"

示例格式：
修改前：{before}
修改后：{after}
理由：{reason}
方法：{method}
```

#### 写作心理训练实现

**场景化训练 Prompt**：
```
你是一位考场写作教练。学生在考场上遇到了以下情况：

{scenarioDescription}

请：
1. 分析这个情况的原因
2. 给出具体的应对策略
3. 提供一个可操作的"急救方案"

语气：冷静、具体、 actionable。
```

#### 考试策略实现

**策略推荐逻辑**：
```typescript
function getExamStrategy(grade: string, abilityLevel: string, subject: string): ExamStrategy {
  const baseStrategy = GRADE_STRATEGIES[grade]
  const adjustedStrategy = adjustByAbility(baseStrategy, abilityLevel)
  const subjectStrategy = applySubjectSpecific(adjustedStrategy, subject)

  return subjectStrategy
}
```

#### 个人弱项档案实现

**弱项识别算法**：
```typescript
async function updateWeakPoints(userId: string, trainingResult: TrainingResult) {
  const existing = await getWeakPoints(userId)
  const newWeakPoints = identifyWeakPoints(trainingResult)

  // 合并弱项
  const merged = mergeWeakPoints(existing, newWeakPoints)

  // 更新弱项档案
  await updateWeakPointArchive(userId, merged)

  // 生成针对性训练推荐
  const recommendations = generateWeakPointRecommendations(merged)

  return recommendations
}

function identifyWeakPoints(result: TrainingResult): WeakPoint[] {
  const weakPoints: WeakPoint[] = []

  // 识别连续低分的能力点
  for (const dimension of result.dimensions) {
    if (dimension.score < 70) {
      weakPoints.push({
        type: dimension.name,
        score: dimension.score,
        frequency: 1,
        description: dimension.feedback
      })
    }
  }

  return weakPoints
}
```

### 13.9 教学支撑模块的技术实现

#### 评分标准显式化实现

**评分标准数据模型**：
```typescript
interface Rubric {
  id: string
  subject: 'chinese' | 'english'
  genre: string  // '议论文' | '记叙文' | '应用文'
  level: string  // '一类卷' | '二类卷' | '三类卷' | '四类卷'
  scoreRange: string  // '54-60'
  criteria: {
    dimension: string  // '立意' | '结构' | '论证' | '语言'
    requirement: string
    positiveExample: string
    negativeExample: string
  }[]
}
```

**标准解读 Prompt**：
```
请用通俗易懂的语言向高中生解释以下评分标准：

维度：{dimension}
要求：{requirement}
正面例子：{positiveExample}
反面例子：{negativeExample}

要求：
1. 用一句话说清楚核心要求
2. 用学生能理解的语言，避免术语
3. 给出具体的改进建议
```

#### 错误类型学实现

**错误分类数据模型**：
```typescript
interface ErrorCategory {
  id: string
  subject: 'chinese' | 'english'
  type: string  // '逻辑类' | '结构类' | '语言类' | '规范类'
  subType: string  // '偷换概念' | '以偏概全' | ...
  description: string
  typicalExample: string
  correctionMethod: string
  relatedLevel: string  // 对应训练层级
}
```

**AI 错误分类 Prompt**：
```
请分析以下作文中的错误，并分类：

{essayContent}

错误分类体系：
- 逻辑类：偷换概念、以偏概全、因果倒置、循环论证
- 结构类：详略不当、虎头蛇尾、段落失衡、缺乏过渡
- 语言类：口语化、用词不当、句式单调
- 规范类：错别字、标点错误、格式问题

请为每个错误：
1. 定位（段落+句子）
2. 分类（类型+子类）
3. 说明为什么是错误
4. 给出修改建议

输出格式：
```json
{
  "errors": [
    {
      "location": "第三段第二句",
      "type": "逻辑类-偷换概念",
      "originalText": "...",
      "explanation": "...",
      "correction": "..."
    }
  ]
}
```
```

#### 写作过程教学实现

**审题教学 Prompt**：
```
请用"审题三步法"引导学生分析以下题目：

题目：{topic}

请：
1. 提取关键词：题目中的核心概念是什么？
2. 识别矛盾：题目在问什么？核心矛盾是什么？
3. 多角度思考：从个人/群体/社会/历史等不同角度，能得出什么立意？

注意：不要直接告诉答案，而是通过提问引导学生思考。
```

**构思教学 Prompt**：
```
请用"提纲编写四问"引导学生构思：

核心论点：{thesis}

请引导学生回答：
1. 我打算从哪几个方面论证？（分论点）
2. 每个分论点用什么论据支撑？（事例/名言/数据）
3. 段落之间如何衔接？（过渡句）
```

**修改教学 Prompt**：
```
请用"修改四步法"指导学生修改：

问题类型：{errorType}
问题描述：{errorDescription}

请：
1. 逻辑检查：论点和论据是否匹配？论证是否有跳跃？
2. 结构检查：段落顺序是否合理？详略是否得当？
3. 语言检查：是否有口语化表达？句式是否单调？
4. 规范检查：错别字、标点、格式

优先级：逻辑 > 结构 > 语言 > 规范
```

#### 文体意识训练实现

**文体检查 Prompt**：
```
学生正在写一篇{genre}。请检查：

{essayContent}

{genre}的核心特征：
{genreCharacteristics}

请检查：
1. 是否符合{genre}的写作要求？
2. 是否有文体混淆的倾向？
3. 给出具体的修正建议。
```

**文体特征数据模型**：
```typescript
interface GenreCharacteristics {
  genre: string
  subject: 'chinese' | 'english'
  coreFeatures: string[]  // 核心特征
  requirements: string[]  // 写作要求
  commonMistakes: string[]  // 常见文体混淆问题
  examFrequency: string  // 高考出题频率
}
```
